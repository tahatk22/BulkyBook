@model IEnumerable<CoverType>
@{
    ViewData["Title"] = "Index";
}
<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary"> Cover List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Cover" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-patch-plus"></i> &ensp; Create New Cover
            </a>
        </div>
    </div>
    <br />
<table class="table table-bordered table-striped" style="width:100%">
    <thead>
        <tr>
            <th>Cover Id</th>
            <th>Cover Name</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td width="50%">@item.Id</td>
                <td width="30%">@item.Name</td>     
                <td><a class="btn btn-primary" asp-controller="Cover" asp-action="Edit" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i>&ensp;Edit</a></td>
                <td><a class="btn btn-danger" asp-controller="Cover" asp-action="Delete" asp-route-id="@item.Id"><i class="bi bi-trash3-fill"></i>&ensp;Delete</a></td>
            </tr>
        }
    </tbody>
</table>
</div>
